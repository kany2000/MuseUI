# MuseUI is configured from the browser UI.
# This file is only a safe place for local development notes.
#
# Do not put real API keys, access tokens, webhook URLs, or private endpoints
# in files that may be committed. Local secret files such as `.env` are ignored
# by Git.

# Example note:
# Gemini official base URL: https://generativelanguage.googleapis.com
