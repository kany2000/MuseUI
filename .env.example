# Optional local defaults for development only.
# The app can be configured entirely from the browser UI.

# Gemini API key, if you want to prefill local development.
VITE_GEMINI_API_KEY=

# Optional custom Gemini-compatible root URL.
# Leave empty to use Google's official Gemini API.
VITE_GEMINI_BASE_URL=
